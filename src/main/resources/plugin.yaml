# Refer https://docs.halo.run/developer-guide/plugin/basics/manifest

apiVersion: plugin.halo.run/v1alpha1
kind: Plugin
metadata:
  # The name defines how the plugin is invoked, A unique name
  name: interaction-plus
spec:
  enabled: true
  # 前端产物仅打包 ui/ 目录，依赖 Halo 2.25 的 prefer-plugin-ui-resources 行为。
  # 安装/启动校验均按 toStableVersion() 比较，">=2.25.0" 在 2.25.0-beta.1 实例可通过；
  # 不可写 ">=2.25.0-beta.1"（java-semver 范围表达式不支持预发布后缀，安装时报错）。
  requires: ">=2.25.0"
  author:
    name: Tim0x0
    website: https://github.com/Tim0x0
  logo: logo.png
  homepage: https://github.com/Tim0x0/halo-plugin-interaction-plus#readme
  repo: https://github.com/Tim0x0/halo-plugin-interaction-plus
  issues: https://github.com/Tim0x0/halo-plugin-interaction-plus/issues
  displayName: "互动增强"
  description: "为 Halo 提供用户装饰、身份标识和前台展示组件。"
  settingName: "interaction-plus-settings"
  configMapName: "interaction-plus-configmap"
  license:
    - name: "GPL-3.0"
      url: "https://github.com/Tim0x0/halo-plugin-interaction-plus/blob/main/LICENSE"
